# React + TypeScript + Vite

# Перевірте статус файлів, які змінилися

git status

# Додайте всі змінені файли до індексу

git add .

# Створіть коміт з описом змін

git commit -m "Ваш опис змін, наприклад: оновлення функціоналу"

## git push origin main

<!-- запускать джесон сервер  -->

запускать джесон сервер
npx json-server --watch db.json
